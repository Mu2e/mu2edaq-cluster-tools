[pytest]
testpaths = tests
addopts = -v --tb=short
# The package lives under src/ (see package-dir in pyproject.toml), so it is
# only importable once installed. Adding src/ here keeps `pytest` working in
# an uninstalled checkout; an editable install takes precedence when present.
pythonpath = src
